settings {
   insist = 1
}

sync {
   default.rsync,
   source = "/vol/projects",
   target = "thip:/vol/projects",
   delay = 1,
   rsync = {
      perms = true,
      _extra = { "--chmod=D775,F664,Fugo+X" }
   }
}
